# Brand assets

Drop the final LoanHub logo file here (e.g. `logo.svg` or `logo.png`) once it's
ready, then import it in `src/components/BrandLogo.tsx` in place of the
temporary `TbBuildingBank` icon mark.
